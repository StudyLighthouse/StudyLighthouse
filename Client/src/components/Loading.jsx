// src/components/Loading.js
import React from "react";
import "./Loading.css"; // Import the CSS file for styling the spinner

const Loading = () => {
  return <span class="center-spinner"></span>;
};

export default Loading;
